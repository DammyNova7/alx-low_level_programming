/**
 * main - Entry point
 * Using printf function
 * Return: Always 0 (Success)
 */

#include <stdio.h>

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\m");
	return (0);
}
