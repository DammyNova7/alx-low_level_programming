#include "main.h"

/**
 * _isupper(int c)-function
 * function is to check for uppercase characters
 * Returns 1 if c is uppercase
 * Returns 0 otherwise
 * Return: 0 success
 */

int _isupper(int c)
{
	if (c <= 'A' && c >= 'Z')
	{
	
		return (1);
	}
	else
	{
		return (0);
	}
	return (0);
}
