#include <stdio.h>

/**
 * main - Entry point of a program
 *
 * prints all digits of base 10 starting from 0
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int i = 0;

	for (; i <= 9; i++)
	{
		printf("%d", i);
	}
		printf("\n");

	return (0);
}
