#include <stdio.h>

/**
 * main - Entry point of a program
 *
 * prints all digits of base 10 starting from 0
 *
 * Return: Always 0 (success)
 */

int main(void)
{
	int i = 48;

	for (; i <= 57; i++)
	{
		putchar(i);
	}
		putchar(10);

	return (0);
}
