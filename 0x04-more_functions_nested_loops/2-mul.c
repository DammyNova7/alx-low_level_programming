#include "main.h"

/**
 * mul(int a, int b)-fuction
 * the function multiplies two integers
 * @a: integer to multiply
 * @b: integer
 * Return: the sum
 */

int mul(int a, int b)
{
	return (a * b);
}
