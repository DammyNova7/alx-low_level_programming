#include "main.h"

/**
 * _isdigit(int c)-function to check for a digit
 * @c: the value to be checked
 * Returns 1 if c is a digit
 * Returns 0 otherwise
 * Return: 0 success
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '9')

	{
		return (1);
	}

	else
	{
		return (0);
	}

	return (0);
}

